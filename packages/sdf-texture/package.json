{
  "name": "@webglify/sdf-texture",
  "version": "0.0.5",
  "private": false,
  "main": "lib/index.js",
  "directories": {
    "dist": "lib"
  },
  "scripts": {
    "build": "ncp src/ lib/ && tsc",
    "watch": "nodemon -e ts,js --watch ./src -x 'npm run build' "
  },
  "dependencies": {
    "@webglify/chain": "^0.1.0",
    "bidi-js": "^1.0.2",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "ncp": "^2.0.0",
    "typescript": ">=3.0.0"
  }
}
